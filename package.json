{
  "name": "mirabo-sdk",
  "private": true,
  "devDependencies": {
    "@types/node": "^17.0.21",
    "lerna": "^4.0.0",
    "typescript": "^4.6.2"
  },
  "scripts": {
    "publish": "lerna run tsc && lerna publish",
    "test": "echo \"Error: run tests from root\" && exit 1"
  }
}
